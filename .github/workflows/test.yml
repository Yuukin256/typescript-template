name: 'test'

on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@v2
        with:
          node-version: '>=16.6.0'
          cache: 'yarn'
      - run: yarn install
      - run: yarn test
